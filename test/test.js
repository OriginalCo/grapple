var should = require('should')
  , schema = require('../')
  
describe('define a simple schema', function() {
  it('should register a Mongoose schema with some primitive datatypes', function(done) {
    done()
  })

})
